import styled from 'styled-components';
import ContentProps from '../Interfaces';

//
const MiscContent = ({ currentIndex, thisIndex }: ContentProps) => {

    return (
        <Wrapper>
            AtomicCRMContent
        </Wrapper>
    );
};

const Wrapper = styled.div`
    height: 100%;
    width: 100%;
    overflow: hidden;
`;

export default MiscContent;